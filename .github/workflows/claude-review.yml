name: Claude PR Review

on:
  workflow_call:
    inputs:
      additional_prompt:
        description: Extra instructions appended to the default review prompt.
        required: false
        type: string
        default: ""
    secrets:
      CLAUDE_CODE_OAUTH_TOKEN:
        required: true

concurrency:
  group: claude-review-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  review:
    name: Claude Review
    runs-on: ubuntu-latest
    if: github.event.pull_request.user.login != 'dependabot[bot]'
    permissions:
      contents: read
      pull-requests: write
      issues: write
      id-token: write

    steps:
      - name: Checkout caller repo
        uses: actions/checkout@v6

      - name: Run Claude Code review
        uses: anthropics/claude-code-action@v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          use_sticky_comment: true
          track_progress: true
          include_fix_links: false
          claude_args: >
            --model opus
            --effort xhigh
            --allowedTools "Bash(gh pr diff:*),Bash(gh pr view:*),Read,Glob,Grep"
          prompt: |
            Do a detailed code review of this pull request following these guidelines.

            ### Actionability requirement

            Every finding — including `Minor:` and `Nit:` — must be actionable: there must be a
            concrete change you are recommending the author make. If there is nothing for the author
            to do, do not write the finding.

            - Never flag something "for awareness", "for visibility", "just flagging", "noting that",
              or "no change needed". If you catch yourself writing any of those, delete the finding.
            - Do not flag pre-existing issues, hypothetical edge cases, or scenarios that are
              unreachable in practice — even to note they are unhandled.
            - A finding that ends by concluding the code is actually fine is not a finding. Drop it.

            ### Bug-flagging criteria

            - It meaningfully impacts the accuracy, performance, security, or maintainability of the code.
            - The bug is discrete and actionable (i.e. not a general issue with the codebase or a combination of multiple issues).
            - The bug was introduced in the commit (pre-existing bugs should not be flagged).
            - The author of the original PR would likely fix the issue if they were made aware of it.
            - It is not enough to speculate that a change may disrupt another part of the codebase, to be considered a bug,
              one must identify the other parts of the code that are provably affected.
            - The bug is clearly not just an intentional change by the original author.

            ### Detailed finding quality

            - The finding should be clear about why the issue is a bug.
            - The finding should appropriately communicate the severity of the issue. It should not claim that an issue is
              more severe than it actually is.
            - The finding should be brief. The body should be at most 1 paragraph. It should not introduce line breaks
              within the natural language flow unless it is necessary for the code fragment.
            - The finding should clearly and explicitly communicate the scenarios, environments, or inputs that are
              necessary for the bug to arise. The finding should immediately indicate that the issue's severity depends
              on these factors.
            - The finding should be written such that the original author can immediately grasp the idea without close
              reading.
            - A short fenced code block is welcome wherever it adds useful context — for example to
              illustrate a suggested fix or the corrected lines. Keep it to the minimum relevant lines,
              and use a plain code fence (not a ```suggestion block, which is inert outside inline reviews).

            ### Severity prefixes

            Findings that pass the bug-flagging criteria above are unprefixed; weaker concerns are `Minor:` or
            `Nit:`.

            - `Nit:` — subjective / personal preference; use sparingly.
            - `Minor:` — real concern but not a bug or behavior change. CLAUDE.md convention violations typically
              fall into this category.
            - No prefix — findings that meet the bug-flagging criteria above.

            Skip formatting and style findings entirely — pre-commit handles those.

            ## Organize the review under these sections:

            - `Summary`
              - A high-level take plus any cross-cutting concerns. One paragraph maximum.
            - `Details`
              - One finding per h4 #### heading, ordered by severity, most severe first.
              - The first line of each heading is the severity prefix in bold — only if the
                finding has one (see Severity prefixes) — followed by the backticked
                location. The explanation and a commit permalink to the line each go on
                their own line indented beneath. Use `path/to/file.ext:LINE` with `#L<LINE>`
                for a single line, or `path/to/file.ext:START-END` with `#L<START>-L<END>`
                for a range (keep the `L` on both ends, e.g. `#L54-L76`); either form is
                valid whether the lines were added, changed, or removed.
              - Anchor the permalink to the commit where the cited lines actually exist:
                - For lines the PR adds, changes, or leaves in place (present at head), use
                  `${{ github.event.pull_request.head.sha }}` with the new line numbers.
                - For lines the PR deletes (absent at head), use
                  `${{ github.event.pull_request.base.sha }}` with the original pre-change
                  line numbers taken from `gh pr diff` — they are gone from the checked-out
                  tree, so do not read them from the working files. Append ` (removed)` to
                  the location of a deletion finding so it is clear the line numbers refer
                  to the pre-change file; leave additions and changes unmarked.

              - Use the two commit SHAs EXACTLY as they appear in the templates below — they are
                already the real head and base SHAs. Copy the SHA character-for-character. Do NOT
                invent, guess, abbreviate, modify, or otherwise hallucinate a SHA, and do not
                substitute a commit hash you saw in `gh pr diff`, `git log`, or anywhere else. The
                only two valid SHAs in a permalink are the head SHA (for added/changed lines) and
                the base SHA (for removed lines) exactly as shown. A permalink with any other SHA
                is wrong even if every other part is correct.

              - There are four location shapes, one per row of the matrix below. Match your
                finding to the correct row and copy that exact shape — do not improvise a hybrid.
                Pick the SHA column by whether the cited lines exist at head (added/changed) or
                only existed before the PR (removed). Pick the anchor column by whether you are
                citing one line or a range. The ` (removed)` suffix and the `${{ github.event.pull_request.base.sha }}`
                SHA always go together; additions and changes use neither.

                | Case | Location (in the `####` heading) | SHA in permalink | Permalink anchor |
                | ---- | -------------------------------- | ---------------- | ---------------- |
                | Single added/changed line | `path/to/file.ext:LINE` | head | `#L<LINE>` |
                | Added/changed range | `path/to/file.ext:START-END` | head | `#L<START>-L<END>` |
                | Single removed line | `path/to/file.ext:LINE` (removed) | base | `#L<LINE>` |
                | Removed range | `path/to/file.ext:START-END` (removed) | base | `#L<START>-L<END>` |

              - The four examples below cover every row of the matrix. Each shows the heading,
                the explanation line, and the permalink line. Find the one whose comment matches
                your finding and follow it exactly.

                <!-- Single added/changed line: head SHA, single-line #L anchor, no (removed), no prefix -->
                #### `path/to/file.ext:42`
                A short paragraph explaining a finding about one added or changed line.
                https://github.com/${{ github.repository }}/blob/${{ github.event.pull_request.head.sha }}/path/to/file.ext#L42

                <!-- Added/changed range: head SHA, #L<START>-L<END> anchor, no (removed). Example carries a Minor: prefix. -->
                #### **Minor**: `path/to/file.ext:54-76`
                A short paragraph explaining a finding that spans several added or changed lines.
                https://github.com/${{ github.repository }}/blob/${{ github.event.pull_request.head.sha }}/path/to/file.ext#L54-L76

                <!-- Single removed line: base SHA, single-line #L anchor, (removed) suffix. Line number is the pre-change number from `gh pr diff`. -->
                #### `path/to/file.ext:108` (removed)
                A short paragraph explaining a finding about one line the PR deletes.
                https://github.com/${{ github.repository }}/blob/${{ github.event.pull_request.base.sha }}/path/to/file.ext#L108

                <!-- Removed range: base SHA, #L<START>-L<END> anchor, (removed) suffix. Example carries a Nit: prefix. -->
                #### **Nit**: `path/to/file.ext:120-134` (removed)
                A short paragraph explaining a finding about several lines the PR deletes.
                https://github.com/${{ github.repository }}/blob/${{ github.event.pull_request.base.sha }}/path/to/file.ext#L120-L134

            Omit any sections that are empty.

            ${{ inputs.additional_prompt }}
